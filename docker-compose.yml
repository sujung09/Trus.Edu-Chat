services:
  backend:
    build: ./backend
    container_name: chat-backend
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: chat-frontend
    ports:
      - "3000:3000"
    restart: unless-stopped
    depends_on:
      - backend
